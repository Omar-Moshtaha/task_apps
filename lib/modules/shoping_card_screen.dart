import 'package:flutter/material.dart';

class ShopingCardScreen extends StatelessWidget {
  const ShopingCardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
